<Window x:Class="TwitchAudioPlayer.WPF.Views.Modals.HotkeySettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:modals="clr-namespace:TwitchAudioPlayer.WPF.ViewModels.Modals"
        mc:Ignorable="d"
        Title="VkSettingsWindow" Height="280" Width="500" ResizeMode="NoResize"
        Style="{StaticResource MaterialDesignWindow}"
        d:DataContext="{d:DesignInstance Type=modals:HotkeySettingsViewModel, IsDesignTimeCreatable=True}"
        WindowStyle="None">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Настройки хоткеев" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" />
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Previous:" VerticalAlignment="Center"
                           HorizontalAlignment="Right" Margin="5" />
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Pause:" VerticalAlignment="Center"
                           HorizontalAlignment="Right" Margin="5" />
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Next:" VerticalAlignment="Center"
                           HorizontalAlignment="Right" Margin="5" />
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Volume Mute:" VerticalAlignment="Center"
                           HorizontalAlignment="Right" Margin="5" />
                <TextBlock Grid.Row="5" Grid.Column="0" Text="Volume Down:" VerticalAlignment="Center"
                           HorizontalAlignment="Right" Margin="5" />
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Volume Up:" VerticalAlignment="Center"
                           HorizontalAlignment="Right" Margin="5" />

                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding PrevText, Mode=TwoWay}"
                         KeyDown="HotkeyTextBox_OnKeyDown" Name="PrevKeyTextBox"
                         VerticalAlignment="Center" MinWidth="250" HorizontalAlignment="Left" Margin="10 0 0 0" />
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PauseText, Mode=TwoWay}"
                         KeyDown="HotkeyTextBox_OnKeyDown" Name="PauseKeyTextBox"
                         VerticalAlignment="Center" MinWidth="250" HorizontalAlignment="Left" Margin="10 0 0 0" />
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding NextText, Mode=TwoWay}"
                         KeyDown="HotkeyTextBox_OnKeyDown" Name="NextKeyTextBox"
                         VerticalAlignment="Center" MinWidth="250" HorizontalAlignment="Left" Margin="10 0 0 0" />
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding VolMuteText, Mode=TwoWay}"
                         KeyDown="HotkeyTextBox_OnKeyDown" Name="VolMuteKeyTextBox"
                         VerticalAlignment="Center" MinWidth="250" HorizontalAlignment="Left" Margin="10 0 0 0" />
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding VolDownText, Mode=TwoWay}"
                         KeyDown="HotkeyTextBox_OnKeyDown" Name="VolDownKeyTextBox"
                         VerticalAlignment="Center" MinWidth="250" HorizontalAlignment="Left" Margin="10 0 0 0" />
                <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding VolUpText, Mode=TwoWay}"
                         KeyDown="HotkeyTextBox_OnKeyDown" Name="VolUpKeyTextBox"
                         VerticalAlignment="Center" MinWidth="250" HorizontalAlignment="Left" Margin="10 0 0 0" />
            </Grid>
        </StackPanel>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,20,0,10">
            <Button Content="Сохранить" Command="{Binding SaveCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,10,0" />
            <Button Content="Отмена" Command="{Binding CancelCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}" />
        </StackPanel>
    </Grid>
</Window>