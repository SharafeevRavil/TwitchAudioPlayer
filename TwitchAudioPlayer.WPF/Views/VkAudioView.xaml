<UserControl x:Class="TwitchAudioPlayer.WPF.Views.VkAudioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TwitchAudioPlayer.WPF.Views"
             xmlns:viewModels="clr-namespace:TwitchAudioPlayer.WPF.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="500"
             d:DataContext="{d:DesignInstance Type=viewModels:VkAudioViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Load Tracks (Default order)" Command="{Binding LoadAudioTracksCommand}"
                    Margin="5" />
            <Button Grid.Column="1" Content="Shuffle Tracks" Command="{Binding ShuffleTracksCommand}" Margin="5 " />
            <Button Grid.Column="2" Command="{Binding SettingsCommand}" Margin="5" 
                    Style="{StaticResource MaterialDesignIconButton}" Width="35" Height="35" > 
                <materialDesign:PackIcon Kind="Settings" />
            </Button>
        </Grid>

        <ListBox Grid.Row="1" ItemsSource="{Binding AudioTrackViewModels}" HorizontalContentAlignment="Stretch" Margin="0 0 5 0" 
                 VerticalAlignment="Stretch" ScrollViewer.ScrollChanged="ScrollViewer_ScrollChanged"
                 VirtualizingPanel.ScrollUnit="Pixel" SelectedItem="{Binding SelectedTrack, Mode=TwoWay}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                    <Setter Property="Padding" Value="0" />
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:AudioTrackControl DataContext="{Binding}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>